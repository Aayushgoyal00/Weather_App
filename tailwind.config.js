/** @type {import('tailwindcss').Config} */
module.exports = {
  purge: ["*"], // Adjust paths as needed
  darkMode: false,   // or 'media' or 'class'

  content: ["*"],
  theme: {
    extend: {},
  },
  plugins: [],
}

